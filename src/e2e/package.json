{
  "name": "@flash/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright e2e + network testing suite for the Meridian-WebRTC overlay",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "playwright test",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}