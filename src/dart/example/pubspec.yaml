name: meridian_example
description: Minimal example app for the meridian_webrtc package.
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  meridian_webrtc:
    path: ..
  flutter_webrtc: ^0.12.0
  uuid: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true